apiVersion: v1
kind: Service
metadata:
  name: devops-demo-api
  labels:
    app: devops-demo-api
spec:
  type: NodePort
  selector:
    app: devops-demo-api
  ports:
    - name: http
      port: 80
      targetPort: http

