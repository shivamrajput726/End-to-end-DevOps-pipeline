apiVersion: v1
kind: Service
metadata:
  name: devops-demo-api
  namespace: devops-demo
  labels:
    app: devops-demo-api
spec:
  type: ClusterIP
  selector:
    app: devops-demo-api
  ports:
    - name: http
      port: 80
      targetPort: http

